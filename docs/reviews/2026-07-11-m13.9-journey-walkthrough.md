# M13.9 Manual Instruments — Journey Walkthrough + 30-Second Legibility Test (2026-07-11)

> **Status: TEMPLATE — awaiting founder-recorded observations.** These are the two MANUAL closeout
> instruments from `docs/plans/2026-07-11-m13.9-closeout-plan.md` Tasks 7 & 8. Per CLAUDE.md ("never
> claim something works because code was written"), the *observed behavior* fields below must be
> filled in by a human walking the live browser — they are deliberately left blank, not pre-answered.
>
> **This 30-second legibility script (Instrument #2) is the first instrument handed to the §13
> validation testers** — it is written to be run verbatim by a quant-literate first-time user.

## Preconditions

- Backend up: `uvicorn quantize.api.app:create_app --factory --host 127.0.0.1 --port 8000` with
  `QUANTIZE_DB_PATH=quantize-demo.db` — **confirmed up (HTTP 200), demo seeded** (ETF Momentum
  Rotation `1111…`, Trend-Filtered Portfolio `3333…`).
- Frontend up: Vite on the `feat/m13.9-closeout` branch (restarted this session). It bound to
  **`http://localhost:5174`** because a stale server from the prior smoke session is still holding
  `:5173` (not responding — ignore it, or kill it and restart on `:5173`). If down,
  `cd web && npm run dev` (Node 24).
- **Fresh profile:** the simplest path — **just open `http://localhost:5174`**: localStorage is keyed
  per origin (host+port), so the new port starts with an empty `quantize.journey.v1` (checklist at
  0/5) and no `quantize.theme` (dark default) with nothing to clear. Alternatively, on any port, clear
  its localStorage (DevTools → Application → Local storage) or use a private window.
- Suggested backtest window: **2025-07-31 → 2025-08-29** (monthly cadence → 1 evaluation).

---

## Instrument #1 — Fresh-profile journey walkthrough (Task 7)

Complete the journey guided **only** by Home + the checklist + on-screen empty states (README closed).
Record *actual observed behavior* in each slot.

### Phase 0 — Arrival
- [ ] Home shows the **"Walk the journey" checklist at 0/5**, all steps unticked.
  - Observed: _______________________________________________
- [ ] The Home **"Walk the journey" card** (Open the demo strategy) is present.
  - Observed: _______________________________________________

### Phase 1 — Walk the five steps (checklist should tick each as performed)
- [ ] **Open the demo** (Home → *ETF Momentum Rotation* → Open the demo strategy) → checklist step 1
      ticks; strategy bar appears (Validate · Run · Save · dataset chip · session stepper).
  - Observed: _______________________________________________
- [ ] **Run a backtest** (dock → Runs → window `2025-07-31..2025-08-29` → Run) → step 2 ticks.
  - Observed: _______________________________________________
- [ ] **Open Results** → step 3 ticks; equity chart + summary render.
  - Observed: _______________________________________________
- [ ] **Open Trace** (click a chart point / evaluation row, or the Trace tab) → step 4 ticks.
  - Observed: _______________________________________________
- [ ] **Extract a component** (Extract component → marquee 2–3 nodes → name → Create) → step 5 ticks.
  - Observed: _______________________________________________
- [ ] **Dismiss** hides the checklist; **reload** the page — ticks persist and it stays dismissed
      (localStorage). (Re-clear localStorage to re-run.)
  - Observed: _______________________________________________

### Phase 2 — Empty states (before performing each action, on a fresh doc)
- [ ] **Runs** tab with no runs → "No runs yet — configure a backtest above and Run."
  - Observed: _______________________________________________
- [ ] **Problems** tab never-validated → "No diagnostics yet — Validate checks the document."
  - Observed: _______________________________________________
- [ ] **Dataset chip** unbound → "Dataset: none — choose" (clicking opens the picker).
  - Observed: _______________________________________________

### Phase 3 — README §4 cross-check
Read each README §4 sentence against the screen; every named control must exist with that name.
Any mismatch → fix README (or the trivial UI copy) and re-verify.
- Result: _______________________________________________

### Phase 4 — F2 closure: M13.8 breadcrumb component navigation (live, never yet exercised)
After extraction:
- [ ] Component appears in the **Library rail → Components** section.
  - Observed: _______________________________________________
- [ ] **Results are unchanged** vs. pre-extraction (same total return / drawdown / final cash).
  - Observed: _______________________________________________
- [ ] Inspector **"Enter component"** → read-only in-canvas view + **breadcrumb** (Strategy ▸ …).
  - Observed: _______________________________________________
- [ ] A **crumb click** and **Esc** each walk back out one level.
  - Observed: _______________________________________________
- [ ] Canvas **double-click** on the ComponentRef enters the component.
  - Observed: _______________________________________________
- [ ] A **trace row inside the component** navigates the breadcrumb to the emitting node's level.
  - Observed: _______________________________________________

### Phase 5 — F3 closure: light-theme sweep
Toggle to light theme (header ☀/☾). Sweep and record anything illegible:
- [ ] Home; canvas cards/edges/legend; Inspector Explanation (formula box); Trace; Results chart;
      **the journey checklist itself** (new this slice).
  - Observed: _______________________________________________

### Phase 6 — F1 visual confirm
- [ ] Results chart **bottom-left corner**: y-min label and start-date no longer collide, in **both**
      themes.
  - Observed: _______________________________________________

**Findings surfaced during the walkthrough** (log all; fix in-slice only if they block an instrument):
- _______________________________________________

---

## Instrument #2 — Scripted 30-second legibility test (Task 8)

Against the **open ETF Momentum Rotation demo in its default view** (dark theme, no run yet unless a
question needs one), a first-time quant-literate user answers the five questions **within ~30 seconds,
without documentation**. Record the *on-screen source* used for each answer. Any answer not
discoverable from the default view ± one click is a **closeout BLOCKER — fix, do not waive.**

1. **Where does data enter the machine?**
   - Answer / on-screen source: _______________________________________________
   - (Expected source: the **Data Source card** + the stage-strip **Data** segment.)
2. **What does each major stage do?**
   - Answer / on-screen source: _______________________________________________
   - (Expected: the **stage strip** + category cards + role-first sentences.)
3. **How are portfolio targets produced?**
   - Answer / on-screen source: _______________________________________________
   - (Expected: Weighting/Risk → **Targets** path to `output.target_portfolio`.)
4. **How does the engine turn targets into orders and fills?**
   - Answer / on-screen source: _______________________________________________
   - (Expected: the **Engine** segment (outside the graph) + Results/Trace Engine grouping + the
     "all runs are simulations over local data" notice.)
5. **Where do I inspect math, parameters, validation, and trace?**
   - Answer / on-screen source: _______________________________________________
   - (Expected: Inspector **Explanation** + **ParamForm** + **Problems** + **Trace**.)

**Verdict:** all five discoverable within one click? ☐ PASS  ☐ BLOCKER (list fixes)
- _______________________________________________

When all five pass, tick the headline DoD box in `docs/plans/2026-07-06-m13-implementation-plan.md`
(line ~50) with the recorded sources.
