# M14.9 Closeout Plan — Verify, Document, and Honestly Close M14 (Opus execution packet)

> **For Claude (Opus):** REQUIRED SUB-SKILL: use `superpowers:executing-plans` (or
> `superpowers:subagent-driven-development` if run in-session) task-by-task. This slice is
> docs + verification; TDD applies only to any fix a broken verification forces. Use
> `superpowers:verification-before-completion` before every done-claim: cite actual command
> output, never "should work."
>
> Plan-of-record: `docs/plans/2026-07-11-m14-implementation-plan.md` — its "### M14.9 —
> Closeout" section (lines 362–380) is normative; this packet operationalizes it against the
> repo as it actually stands on 2026-07-14. Design record:
> `docs/plans/2026-07-11-m14-behavior-legibility-design.md` (D-1…D-4 binding; §7
> flip-triggers, §9 M14.3, §10 stop conditions).

**Goal:** verify the full Node Value Tap journey live against the demo DB, record real
test/gate evidence, update README/LEARNING_LOG, tick the M14 definition-of-done with
evidence, write the closeout report + risk register, and produce an evidence-based
recommendation on the M14.3 gate — then stop for founder review and the Codex/Fable final
M14 review.

**Architecture:** this is a docs + verification slice. Admissible code changes are exactly
two enumerated kinds: (1) the **pre-authorized, bounded Task 1.5 remediation set** (golden-
anchored Strategy A/B tap tests, a depth≥2 nested-component tap test, and the latency-log
observability fix — nothing else), and (2) a TDD-driven fix to something the live
walkthrough proves broken *and* that blocks a closeout claim. Everything else is docs,
screenshots, and recorded evidence. Any code change outside those two kinds = stop and
report.

**Execution-date convention:** `<EXEC-DATE>` = the day Task 0 actually runs (YYYY-MM-DD).
Every dated artifact this plan creates — the closeout report, the screens directory, branch
evidence references — uses `<EXEC-DATE>`, substituted consistently everywhere. This plan's
own filename keeps its authoring date; do not rename it.

**Explicitly out of scope (stop conditions, not suggestions):** implementing M14.3
edge-hover; capture-at-run or any caching/memoization; value-over-time; any contract-shape
change; any new evaluation path; M15/M16 work; broadening M14. Design §10 applies verbatim.

---

## 0. Facts the implementer must know (verified against the repo, 2026-07-14)

1. **Everything before M14.9 is merged to `main`.** `main` = `ae43ca0` (PR #25, M14.2).
   M14.1a/1a′/1b/1c landed via PR #23 (`a3ce5eb`) + review-fix PR #24 (`2d49c13`); M14.2a/2b
   plus eleven `fix(m14.2*)`/PX commits landed via PR #25. There is no in-flight branch;
   `feat/m14.9-closeout` does not exist yet.
2. **The tracked tree is clean; the untracked inventory is a closeout item:**
   - `m142-smoke-01..05-*.png` — five M14.2 Task-6 smoke screenshots sitting at **repo
     root** (cap-portfolio-targets, honest-404-v1-run, component-exposed-output,
     inner-node-trailing-return, noeval-session). No M14 review doc references them yet.
   - `docs/reviews/2026-07-11-m13.9-screens/m139-*.png` — **15 screenshots referenced 22
     times by the committed** `docs/reviews/2026-07-11-m13.9-journey-walkthrough.md` but
     never committed (only `m139-o3-inner-node-readonly.png` is tracked). This is a broken
     evidence trail from M13.9 to repair here.
   - `.claude/`, `.playwright-mcp/` — local tooling; leave untracked (do NOT commit; add to
     `.gitignore` only if the founder approves — default: leave as-is).
   - **This plan file itself** (`docs/plans/2026-07-14-m14.9-closeout-plan.md`) is untracked
     too — Task 0 adopts it as the branch's first commit (the repo's convention: planning
     docs are committed, e.g. `db4b167`). It is expected inventory, not a stop condition.
3. **Code seams (all merged, verified):** service `quantize/valuetap/service.py` (logger
   `quantize.valuetap` at line 55; per-tap `elapsed_ms` log at line 230 — this is the
   flip-trigger-3 instrument); summarization `quantize/runtime/summarize.py:100`
   (`summarize(RuntimeValue) -> ValueSummary`); DTO `quantize/api/dto/values.py`; endpoint
   on the runs router `quantize/api/routes/runs.py`; web rendering
   `web/src/components/Inspector.tsx` (module-private `ValueBlock` in the "At session"
   slot); client fetch in the API client module (`getNodeValue`).
   **Plan drift to record in the closeout report:** the plan said `quantize/valuetap/summarize.py`;
   it shipped as `quantize/runtime/summarize.py` with the DTO as its wire mirror. Record it;
   do not "fix" it.
4. **Test files:** `tests/test_valuetap_service.py`, `tests/test_valuetap_crosscheck.py`,
   `tests/test_valuetap_summarize.py`, `tests/api/test_values_endpoint.py`,
   `tests/valuetap_helpers.py`; web `Inspector.values.test.tsx`,
   `Inspector.atsession.test.tsx` (+ extended `Inspector.componentNode` /
   `App.componentInspect` suites).
5. **Counts baseline:** M14.2 plan recorded 1021 py / 598 web *before* M14.2; web grew
   during M14.2. Get the real numbers from the gate (Task 1) — never guess or reuse these.
6. **README `Status` is stale** (`README.md:127–135`): says M13.9, "985 Python tests / 598
   web tests across 62 files". Refresh to M14 + Task-1.6 actuals.
7. **LEARNING_LOG:** last entry is "## M13.9 — Read-only component internals …" at
   `docs/LEARNING_LOG.md:1440`. Append the M14 entry after it, matching the existing entry
   shape (concepts → where in repo → reading path → hand exercise → honest verification).
8. **M14 definition-of-done checklist** lives at
   `docs/plans/2026-07-11-m14-implementation-plan.md:38–64` — 12 unticked boxes. The
   closeout ticks each with a one-line evidence note (test file / command output / commit),
   exactly like the M13.9 DoD evidence pass.
9. **Demo DB + smoke procedure (M14.2 plan Task 6 is the template):** never run against the
   real `quantize-demo.db` — copy it to a temp path, start
   `uvicorn quantize.api.app:create_app --factory --host 127.0.0.1 --port 8000` with
   `QUANTIZE_DB_PATH=<copy>`, and `npm run dev` in `web/` (Node-24 shell). Re-seed with
   `python scripts/seed_demo.py` only if the DB is missing/broken. Dev servers may already
   be running from earlier sessions — check ports 8000/5173 first and kill/own them before
   pointing anything at a temp copy.
10. **§13 external validation: no repository evidence; actual status unconfirmed.**
    `docs/reviews/` contains no tester-session notes; nothing in git history records a §13
    session. That proves only that nothing was *recorded in the repo* — a founder-led
    off-repo session or notes would not appear in these greps. Verify the absence yourself
    (grep `docs/reviews/`, `git log --all -i --grep=valid`), record the status as "no
    repository evidence; unconfirmed — founder to confirm at review", and put the explicit
    question in the PR body. Per D-4 the M14.3 gate opens only on §13 feedback; the gate
    recommendation stays closed either way (Task 8).
11. **Node toolchain:** run `./scripts/node24.ps1` in the same shell before any
    npm/node/tsc/codegen command. Full canonical gate: `./scripts/gate.ps1` (needs
    `web/node_modules`; run `npm ci` in `web/` once if missing).
12. **Latency evidence — two known gaps, fixed in Task 1.5 before the walkthrough:**
    (a) *Observability:* `quantize.valuetap` is an ordinary INFO logger
    (`service.py:55`); Uvicorn's default logging config only configures `uvicorn.*`
    loggers and leaves the root logger unconfigured, so the `elapsed_ms` INFO lines do
    **not** appear under the plain `uvicorn …` command. The smoke server must be started
    with an explicit logging config (exact command in Task 2 setup) and a tap log line
    must be *seen* before the walkthrough proper starts.
    (b) *Coverage:* the log call sits after every refusal raise (`service.py:229–236`) —
    it fires only on successful resolution, while the DoD says "per request". Task 1.5(c)
    resolves this (default: extend the log to refusal outcomes; D-28).
    During the walkthrough, capture actual `value tap … elapsed_ms=…` lines and quote them
    in the closeout report with the flip-trigger-3 verdict — at demo scale they should be
    far below the ~1s trigger; say what they actually were.
13. **Demo-DB run versions (recorded at M14.2 smoke — re-verify against the live DB):** runs
    `932806df` / `c9be9809` pin strategy **v1** (pre-extraction — no Momentum Rank
    component); run `e61d2c17` pins **v2**. Only the v2 run can tap the component
    instance/internals; tapping component addresses on a v1 run is the honest-404 case.
14. **Open founder decision explicitly queued for M14.9 — canonical number display.** Three
    formatters coexist: Inspector `fmtValue` (trims), TraceView `String()` (raw), ResultsView
    `toFixed(4)` (untrimmed) — the same panel can show `99999.9975` above `99999.99751234`,
    and cross-panel `126` vs `126.0000`; sub-`0.00005` served values display as bare `0`
    (verbatim only in the title attribute). This is a *presentation-consistency* decision,
    not a derivation question. M14.9 surfaces it (Task 7/PR) with a recommendation; it does
    **not** implement a unification unless the founder decides during review.
15. **Two DoD boxes lack test evidence today (verified 2026-07-15) — Task 1.5 repairs them
    before anything is ticked:**
    - *"Tapped value equals the run's value for **both** reference strategies, asserted
      against the existing committed goldens"* — the valuetap suites
      (`tests/test_valuetap_service.py`, `tests/api/test_values_endpoint.py`) seed only
      Strategy A shapes and a synthetic component; they contain **zero** references to
      `strategy_b` or to `tests/goldens/`.
    - *"A node inside a component (any depth) is tappable"* — only single-segment
      `("mom",)` paths are exercised; no depth≥2 test exists.
    Raw materials for the repair (all committed): `tests/goldens/strategy_a_backtest.json`
    + `strategy_b_backtest.json` with their consumer `tests/test_reference_backtests.py`
    and `tests/golden_utils.py`. For the depth-2 test, the *wiring* precedents in
    `tests/test_component_evaluation.py` are
    `test_nested_components_evaluate_compositionally` (proves the two-level address
    `("outer", "inner_scaler")` + inner node) and
    `test_trace_paths_go_two_component_levels_deep` (asserts `component_path
    ("nt", "tin")`) — but the tracer fixtures use the test-only `test.trace` node type,
    which the value-tap recompute cannot evaluate (it pins `build_core_catalog()`,
    `service.py:178`); Task 1.5(b) builds the depth-2 shape from registered core node
    types instead.
16. **Deferred web cleanups already on record (from the M14.2 final review — carry them into
    the Task-7 register verbatim):** sparkline axis labels lack the title-verbatim fallback;
    five hand-repeated `AtSessionSection` placements (convention, not mechanism); CSS
    reach-in to chart internals (a height/variant prop is the fix); `abbrev` byte-dup
    (DataSourceCard should import from `format.ts`); no web value-tap test-helper module
    (the Python `tests/valuetap_helpers.py` precedent); note-only eager render of collapsed
    details rows; three App test files using local `Entry` mock types instead of importing
    `ComponentTrailEntry`.

---

### Task 0 — Branch + repo/branch audit

1. Run and record verbatim in your working notes (these open the closeout report later):
   ```bash
   git status --short
   git branch --show-current
   git log --oneline -12
   git diff --stat
   git fetch origin && git log --oneline origin/main -1
   ```
   Expected: on `main` = `ae43ca0` = `origin/main`, no tracked modifications, the untracked
   inventory from Fact 2 (which includes this plan file). Anything else → stop and report
   before proceeding.
2. Record `<EXEC-DATE>` = today, and use it for every dated artifact from here on.
3. `git checkout -b feat/m14.9-closeout origin/main`.
4. Adopt the plan: `git add docs/plans/2026-07-14-m14.9-closeout-plan.md` and commit —
   `M14.9: adopt the closeout execution plan`.
5. Confirm the M14.1/M14.2 PR trail (`git log --oneline -40` — PRs #23/#24/#25 present).

### Task 1 — Full gate: real numbers first

1. From repo root: `./scripts/gate.ps1`. Expected: every stage green.
2. Record the exact pytest count, web test count + file count, and each stage's PASS line
   — **as the pre-remediation baseline only**. Task 1.5 adds tests, so every *published*
   count (README, LEARNING_LOG, closeout report) comes from the Task-1.6 post-remediation
   gate, never from here.
3. If any stage is red on an untouched `main` checkout: stop. That is a closeout-blocking
   finding — diagnose with `superpowers:systematic-debugging`, fix TDD-first on this branch,
   and record it in the closeout report. Do not proceed to live verification on a red gate.

### Task 1.5 — DoD evidence audit + bounded test remediation (pre-authorized; TDD)

First, audit **all 12** DoD boxes (`docs/plans/2026-07-11-m14-implementation-plan.md:38–64`)
against existing evidence and write the gap list into your working notes — do this *before*
the walkthrough so no gap is discovered after evidence-gathering is "done". Two gaps are
already known (Fact 15) and one instrument gap (Fact 12); their repairs are pre-authorized
below. **Any further gap the audit finds: stop and report before writing code** — it is a
scope decision, not an automatic fix.

**(a) Golden-anchored reference-strategy taps** — Test:
`tests/test_valuetap_service.py` (extend) or a sibling `tests/test_valuetap_goldens.py`.
Following the seeding pattern of `tests/test_reference_backtests.py` (fixtures +
`golden_utils.py`): run Strategy A and Strategy B over the committed fixture, then tap —
Strategy A: `ret` (trailing return), `rk` (rank), and **`cap`** (`risk.max_weight` — the
final PortfolioTargets-producing node); Strategy B: `gt` (the Boolean trend filter) and
**`mask`** (`portfolio.apply_mask`) — at a fixture session each, asserting the served
values equal the numbers the committed goldens record
(`tests/goldens/strategy_a_backtest.json` / `strategy_b_backtest.json`). **Never address
the `tp` terminal (`output.target_portfolio`):** it deliberately exposes no output ports
and produces an empty mapping (`quantize/nodes/output.py`) — tapping it is
`value_address_not_found` by design; the tappable targets are `cap`/`mask`, and those are
what the goldens' targets compare against. **Golden policy (plan-of-record test blueprint,
M14 plan:384):** existing backtest goldens record targets/valuations/orders only
(`tests/golden_utils.py`) — so `cap`/`mask` assert against them, while `ret`/`rk`/`gt`
appear in **no** existing golden, and their trace events record asset names, not values,
so trace facts cannot anchor them either. For those, **mint committed value-tap goldens**
(a new `tests/goldens/` file per the repo's golden discipline: deterministic fixture →
committed JSON, regenerated only via the established `--update-goldens` flow, number
changes are reviewed diffs). Where a value is also cheaply hand-derivable or
trace-cross-checkable, add that as a *complementary* independent assertion — but the
committed golden is the anchor, not a substitute. RED → GREEN per test.

**(b) Depth≥2 nested tap, core nodes only** — Test: extend `tests/test_valuetap_service.py`
(+ `tests/valuetap_helpers.py`). The recompute always evaluates with `build_core_catalog()`
(`quantize/valuetap/service.py:178`), so `tests/component_fixtures.py::nested_tracer_definition`
is **unusable here** — its innermost node is the test-only `test.trace` type. Instead,
build the depth-2 shape from **registered core node types only** (no catalog monkeypatch —
production-path evidence is the point): extend the existing valuetap seeding helper with an
outer `ComponentDefinition` whose graph is a `ComponentRef` to the already-seeded inner
component, wiring the outer exposed input/output through the ref (the structural
precedents for two-level paths are `test_nested_components_evaluate_compositionally`,
which proves the `("outer", "inner_scaler")` address, and
`test_trace_paths_go_two_component_levels_deep`, which asserts `component_path
("nt", "tin")` — copy their wiring shape, not their node types). Seed a run
whose strategy instantiates the outer component; tap the innermost core node with the
two-segment path and assert it equals the compositional evaluation's value. RED → GREEN.

**(c) Latency-log observability (Fact 12, D-28 default)** — Modify
`quantize/valuetap/service.py`; test: `tests/test_valuetap_service.py` (extend, use
`caplog`). Default per D-28: one `elapsed_ms` line per **service resolution attempt** —
keep the success line as-is and wrap the resolve body so *every* exit logs before
propagating: typed `ValueTapError` refusals carry their outcome `code`, and
**`PersistenceError` exits too** (the unknown-run lookup at `service.py:146` raises before
any ValueTapError path — it must not escape the instrument). Route-layer 422s (malformed
params that never reach the service) are **defined as outside the instrument** — they
trigger no recompute, so they carry no latency signal; state that boundary explicitly in
the Task-4 evidence note for the DoD latency box. No behavior change to responses; no new
logger names. Tests: a successful tap emits exactly one `value tap … elapsed_ms=` record
on `quantize.valuetap`; a refused tap (e.g. unknown node) emits one carrying its code; an
unknown-run tap (PersistenceError) emits one. RED → GREEN. If the founder vetoes D-28,
instead amend the DoD line 64 wording to precisely describe what is logged (successful
resolutions only) with a dated honesty note and record attempt-level logging as a deferred
cleanup — one of the two must happen; ticking the box over the mismatch is not an option.

Then: full `pytest`, `ruff check .`, `ruff format --check .`, `mypy` — clean. Commits per
RED→GREEN pair (`test(m14.9): …` / `fix(m14.9): …`). **Boundary:** these three items are
the entire pre-authorized set — no endpoint, DTO, web, or contract change is admissible
here.

### Task 1.6 — Post-remediation full gate (the published numbers)

Run `./scripts/gate.ps1` again after Task 1.5 lands. Expected: all stages green, pytest
count risen from the Task-1 baseline, web unchanged. **These counts are the ones README
(Task 5), LEARNING_LOG (Task 6), and the closeout report (Task 7) publish.** Task 9's
final gate must reproduce them exactly; if it doesn't (e.g. a walkthrough-blocking fix
added tests), update every published count to Task 9's numbers before opening the PR —
the README, the LEARNING_LOG entry, the report, and the final gate must all agree on one
set of numbers.

### Task 2 — Live verification: the full Node Value Tap journey (the heart of M14.9)

**Files:** create `docs/reviews/<EXEC-DATE>-m14-closeout.md` (walkthrough section first;
grows through Tasks 4–8), screenshots into `docs/reviews/<EXEC-DATE>-m14-closeout-screens/`.

Setup per Fact 9 (temp DB copy; Playwright MCP or manual browser — record which), **but
start the API server with an explicit logging config** — plain `uvicorn …` silences the
`elapsed_ms` lines (Fact 12a). In a shell with `QUANTIZE_DB_PATH=<temp copy>`:

```
python -c "import logging, uvicorn; logging.basicConfig(level=logging.INFO, format='%(asctime)s %(name)s %(levelname)s %(message)s'); uvicorn.run('quantize.api.app:create_app', factory=True, host='127.0.0.1', port=8000)"
```

(`basicConfig` puts an INFO handler on the root logger; Uvicorn's dictConfig keeps
`disable_existing_loggers=False` and leaves root alone, so `quantize.valuetap` INFO
propagates.) **Setup gate — ordered strictly:** start the server, run step 1's provenance
check (run reads only, no taps), and only then issue one tap and confirm a
`value tap … elapsed_ms=` line appears in the console before starting the walkthrough
proper — if it does not, stop and diagnose; do not walk through without the instrument.
Then, recording *actual observed behavior* per step:

1. **Provenance pre-check (plan step 1, mandatory before any tap):** confirm the demo run(s)
   carry `recorded` provenance / `replay_verifiable: true` via the run read
   (`GET /v1/runs/{id}`). Also confirm the run→version mapping from Fact 13 still holds
   (which runs pin v1 vs v2). If a demo run has unknown provenance, re-run the backtest
   in-app to mint a tappable run and note that in the walkthrough — do not silently swap DBs.
2. **Top-level tap:** open the ETF Momentum Rotation demo → select the **v2 run** (Fact 13)
   → set the session cursor to a known evaluated session → click a **top-level** node:
   `sel` (`portfolio.select_top_n`) or `px` (`data.price`). (The v2 top level is `u`, `px`,
   the Momentum Rank component instance, `sel`, `ew`, `cap`, `tp` — verified against the
   demo DB 2026-07-15; `ret`/`rk` live *inside* the component, so there is no top-level
   Trailing Return to tap.) Verify the "At session" section shows the served
   `value_summary` + per-asset values above the trace facts, in served order, with the
   port label visible. Screenshot.
3. **ComponentRef exposed-port tap:** click the Momentum Rank component instance → its
   exposed output's value renders. Screenshot.
4. **Inner-node tap (component internals):** enter the component (breadcrumb appears) →
   select the inner `ret` (Trailing Return) → its value renders, addressed by the trail's
   `component_path`. Screenshot.
5. **Honest refusals:** (a) move the cursor to a non-evaluated session → the recorded
   no-eval note renders verbatim, and (b) the v1-run honest-404: select a **v1 run**
   (Fact 13) and tap an address that doesn't exist in that run's pinned version. Screenshots
   for both. No empty-200s, no blank sections, no client-side guessing.
6. **Targets node:** tap the final PortfolioTargets-producing node — `cap`
   (`risk.max_weight`) in the demo's Strategy-A shape (`portfolio_targets` kind) — weights
   + served `cash` remainder render; confirm the client shows cash only because the server
   served it. Do **not** expect a value on the `tp` terminal (`output.target_portfolio`) —
   it has no output ports by design; if you tap it, the honest `value_address_not_found`
   refusal is the *correct* behavior (worth one line in the walkthrough doc). Screenshot.
7. **Latency evidence:** copy 3–5 actual `elapsed_ms` log lines from the uvicorn console
   covering the taps above (Fact 12). Quote them in the doc with the flip-trigger-3 verdict.
8. **Both themes:** repeat one representative tap in light theme; record legibility.
9. Stop both servers; delete the temp DB copy.

**Rule:** any step that does not behave as documented is a closeout blocker — fix (TDD)
if it blocks an M14 DoD claim, otherwise record it honestly as a known issue with a
deferral target. Never tick a DoD box on aspiration.

Commit: `M14.9: live value-tap walkthrough evidence (doc + screenshots)`.

### Task 3 — Evidence hygiene: adopt the orphaned screenshots

1. Move the five root `m142-smoke-*.png` into
   `docs/reviews/<EXEC-DATE>-m14-closeout-screens/` (rename to match its naming), reference
   each once from the walkthrough doc's "M14.2 smoke (pre-closeout) evidence" subsection,
   and commit them. If any duplicates a fresh Task-2 screenshot, keep the fresh one and
   delete the stale file — say so in the commit message.
2. Commit the 15 untracked `docs/reviews/2026-07-11-m13.9-screens/m139-*.png` that the
   committed M13.9 walkthrough already references (Fact 2) —
   `M14.9: commit M13.9 walkthrough screenshots the review doc references (evidence-trail repair)`.
3. Leave `.claude/` and `.playwright-mcp/` untracked (D-25 default).

### Task 4 — M14 definition-of-done evidence pass

**Files:** modify `docs/plans/2026-07-11-m14-implementation-plan.md` (lines 38–64).

Tick each of the **12** DoD boxes with a one-line evidence note (test file::test name, gate
line, commit hash, or walkthrough-doc anchor). The two boxes Fact 15 flagged cite their new
Task-1.5 tests; the latency box cites the Task-1.5(c) outcome (extended log, or the amended
wording — whichever D-28 resolved to). Three need fresh judgment, not just citation:

- **"No numeric derivation exists in web/"** — the M14.2 PX pass added display formatting
  (`fmtValue`, sparkline axis labels). Re-read the value-rendering path in
  `web/src/components/Inspector.tsx` and confirm formatting-only (no arithmetic, no
  comparison-derived order/emphasis, exponent digits preserved per `05ae5b6`), and cite the
  asserting tests. If anything crosses from formatting into derivation, that is a stop
  condition — report it, don't paper over it.
- **"Tap latency is logged server-side"** — cite the Task-1.5(c) log implementation + its
  `caplog` tests *and* the Task-2 observed console lines (both are needed: the test proves
  the emit, the console lines prove observability in the real server).
- **"Live walkthrough"-dependent boxes** — cite the Task-2 doc sections/screenshots.

Any box that cannot be honestly ticked stays open with a one-line reason — an open box plus
the truth beats a decorated lie. Commit: `M14.9: M14 DoD evidence pass`.

### Task 5 — README refresh

**Files:** modify `README.md`.

1. Status section (lines 127–135): M14 shipped (Node Value Tap — recompute-on-demand,
   `GET /v1/runs/{run_id}/values`, Inspector "At session" values incl. component internals);
   real Task-1.6 counts.
2. Inspection-path touch-up only if the README's described click-path now omits the value
   tap (read §4/journey text first; if it already reads correctly, record "no change
   needed"). Every named control must exist on screen with that name — verify against the
   Task-2 session, not memory.

Commit: `M14.9: README status + inspection-path refresh (M14)`.

### Task 6 — LEARNING_LOG entry

**Files:** modify `docs/LEARNING_LOG.md` (append after line 1440's M13.9 entry, same shape).

Per the plan-of-record step 2: recompute-vs-capture as a worked tradeoff (D-1/D-2, the four
flip-triggers and why none fired); the `outputs`-store projection (evaluator `ValueStore`
key `((*component_path, node_id), output_port)` → summarizer → frozen DTO); provenance
honesty (`captured: false`, fingerprint resolution, unknown-provenance refusal); reading
path (`quantize/api/routes/runs.py` → `quantize/valuetap/service.py` →
`quantize/runtime/summarize.py` → `quantize/api/dto/values.py` →
`web/src/components/Inspector.tsx`); one hand exercise — suggested: pick a session, predict
the Trailing Return node's value for one asset from the formula + raw fixture data *before*
tapping it, then tap and compare (on the v2 run `ret` lives inside the Momentum Rank
component — enter it via the breadcrumb first); honest verification section with Task-1.6
numbers.

Commit: `M14.9: LEARNING_LOG M14 entry`.

### Task 7 — Closeout report + risk/deferral register

**Files:** extend `docs/reviews/<EXEC-DATE>-m14-closeout.md`.

Sections (this document is what the Codex/Fable final review consumes — write for a
skeptical external reviewer):

1. **What M14 shipped** — slice-by-slice with PR/commit hashes (#23/#24/#25 trail).
2. **Evidence** — the Task-1 baseline and Task-1.6 published gate output (exact counts), Task-2 walkthrough results incl.
   latency lines, screenshot index.
3. **DoD checklist** — mirror of the Task-4 ticks with evidence notes; list any box left
   open and why.
4. **Flip-trigger status (design §7)** — all four, each with its status and the evidence
   (1: no stateful nodes exist; 2: no value-over-time requirement surfaced; 3: measured
   `elapsed_ms` vs ~1s; 4: datasets remain content-addressed/immutable). Expected verdict:
   none fired → capture-at-run stays inadmissible.
5. **Plan-drift notes** — honest deltas between plan and shipped code (at minimum the
   `summarize.py` location, Fact 3; add anything else found during Tasks 2/4).
6. **Known issues + deferred-work register** — anything Task 2 surfaced but didn't fix;
   the recorded M14.2 web cleanups (Fact 16, verbatim); plus the standing deferrals with
   their targets: capture-at-run (trigger-gated), value-over-time (future decision), M14.3
   (see §8 below), batch/caching (measured-need gated), engine-boundary values (never via
   the tap).
6b. **Queued founder decision: canonical number display (Fact 14)** — its own subsection:
   show the three-formatter inconsistency with one concrete screenshot/example from Task 2,
   state the options (unify on one display formatter vs. leave per-surface), give a
   recommendation, and mark it *awaiting founder decision — not implemented in M14.9*.
7. **Risks** — at minimum: the engine-drift window (recompute after an engine upgrade can
   409 until a re-run; the cross-check is the tripwire, re-run is the remedy); recompute
   cost growth on real datasets (trigger 3 is the instrument); no confirmed/recorded §13 evidence means
   the legibility thesis remains internally validated only.
8. **§13 interaction notes** — record the status exactly as Fact 10 frames it ("no
   repository evidence; unconfirmed — founder to confirm at review"), put the explicit
   question to the founder, and note that the M13.9 instruments (30-second script, journey
   checklist) plus the now-inspectable values are ready for testers.

Commit: `M14.9: closeout report (evidence, flip-triggers, risks, deferrals)`.

### Task 8 — M14.3 gate decision memo (recommend, don't decide)

**Files:** a "M14.3 gate" section inside the closeout report.

D-4 is binding: M14.3 (edge-hover dataflow, design §9) is gated **on §13 feedback**. The
memo applies that rule to the evidence:

1. **State the gate inputs found:** (a) §13 tester feedback: no repository evidence,
   actual status unconfirmed (Fact 10) — the memo asks the founder to confirm, and only a
   *confirmed* §13 signal can ever open the gate; (b) internal evidence from Task 2 on whether the
   Inspector surface answered "what flows where" during the walkthrough (record concrete
   moments, e.g. "reading an edge's value required clicking its source node — N extra
   clicks", or "never felt missing"); (c) latency measurements (edge-hover multiplies
   request volume; note whether measured `elapsed_ms` would tolerate hover-rate traffic).
2. **Apply the rule:** no *confirmed* §13 feedback ⇒ **the gate stays closed** — that is
   the expected recommendation, and it holds whether §13 hasn't run or simply wasn't
   recorded. Internal impressions may be *recorded* as pre-registered hypotheses for §13
   but cannot open a gate the founder tied to external testers.
3. **Recommend the next slice** for the founder's decision, with reasoning — expected shape:
   run §13 external validation next (the product now has the full select-run → click-node →
   see-value loop testers need; the instruments exist; D-3 already prioritized it), and let
   its findings decide M14.3 vs. M15-direction. If Task-2 evidence genuinely contradicts
   "Inspector is enough" (e.g. the walkthrough repeatedly needed dataflow-at-a-glance),
   say so plainly and recommend the founder weigh opening the gate — but the decision
   stays with the founder either way.

Commit folded into Task 7's report commit or its own `M14.9: M14.3 gate memo`.

### Task 9 — Final gate, PR, stop

1. Re-run `./scripts/gate.ps1` → green; record final counts (they must match README/report).
2. Push `feat/m14.9-closeout`; open PR to `main` titled
   `M14.9: M14 closeout — live verification, evidence, docs, M14.3 gate memo`. PR body: the
   closeout report's summary + evidence links, the explicit NOT-done list (M14.3, §13
   execution, capture-at-run, value-over-time, M15/M16), the §13-status question to the
   founder (Fact 10), and a pointer for the Codex/Fable reviewer to
   `docs/reviews/<EXEC-DATE>-m14-closeout.md`.
3. **Stop.** Founder reviews/merges; the Codex/Fable final M14 review runs against this PR.
   Do not start M14.3, §13, M15, or M16 work.

---

## Founder decision points (defaults embedded — veto before the consuming task)

| # | Decision | Default | Consuming task |
|---|---|---|---|
| D-22 | Repair the M13.9 evidence trail by committing the 15 referenced `m139-*.png` | Yes — the committed doc references them 22× | 3 |
| D-23 | Root `m142-smoke-*.png` disposition | Move into the closeout screens dir; delete any superseded by fresh Task-2 shots | 3 |
| D-24 | M14.3 recommendation absent §13 feedback | Stays gated; recommend §13 as the next move | 8 |
| D-25 | `.claude/` / `.playwright-mcp/` | Leave untracked; no `.gitignore` change | 3 |
| D-26 | Evidence artifact dating | All new dated artifacts use `<EXEC-DATE>` (Task-0 day), applied consistently in Tasks 2, 3, 7, 9 | 2, 3, 7, 9 |
| D-27 | Canonical number display (Fact 14) | Surface with recommendation in the closeout report; implement nothing until the founder decides | 7 |
| D-28 | Latency-log DoD mismatch (Fact 12b) | Extend the log to refusal outcomes (one `elapsed_ms` line per resolution attempt, carrying the outcome code); veto ⇒ amend the DoD wording honestly instead | 1.5 |

## Risks / watch-outs

- **Verification theater is the failure mode of a closeout slice.** Every claim in the
  report must trace to a command output, a screenshot, or a file:line. If the evidence
  isn't there, the claim isn't either.
- **The walkthrough may surface real bugs.** Fix in-slice (TDD) only what blocks an M14
  DoD claim; everything else goes in the known-issues register with a deferral target.
- **Do not "tidy" product code while passing through** — no refactors, no copy churn, no
  formatting drive-bys. Outside Task 1.5's enumerated set and walkthrough-blocking fixes,
  this branch is `docs/` + screenshots only.
- **Port hygiene:** stale dev servers from previous sessions may hold 8000/5173 and point
  at the real demo DB. Verify what is running before the smoke; never mutate
  `quantize-demo.db` itself.
- **Stop-condition vigilance:** if any Task tempts you toward caching, a second evaluation
  path, client-side derivation, contract drift, or M14.3 implementation — halt and report
  (design §10; the M14.2 plan's operationalized list applies).
